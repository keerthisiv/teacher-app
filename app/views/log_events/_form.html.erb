<%= form_for(log_event) do |f| %>
  <% if log_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(log_event.errors.count, "error") %> prohibited this log_event from being saved:</h2>

      <ul>
      <% log_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :teacher %>
    <%= f.text_field :teacher %>
  </div>

  <div class="field">
    <%= f.label :event_time %>
    <%= f.datetime_select :event_time %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :event_type %>
    <%= f.text_field :event_type %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
